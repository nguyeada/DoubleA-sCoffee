<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.2/dist/css/bootstrap.min.css" rel="stylesheet">
        <title>Double A Coffee Database</title>
    </head>
    <body>
        <div class="container">
            <nav class="row navbar navbar-expand-sm py-5">
                <div class="container-fluid">
                    <h3 class="navbar-text">Double A Coffee Company</h3>
                    <ul class="nav nav-pills justify-content-end">                
                        <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
                        <li class="nav-item"><a class="nav-link" href="/drinks">Drinks</a></li>
                        <li class="nav-item"><a class="nav-link" href="/locations">Locations</a></li>
                        <li class="nav-item"><a class="nav-link" href="/customers">Customers</a></li>
                        <li class="nav-item"><a class="nav-link active" href="/orders">Orders</a></li>
                        <li class="nav-item"><a class="nav-link" href="/drink-orders">Drink Tracker: Orders</a></li>
                        <li class="nav-item"><a class="nav-link" href="/drink-locations">Drink Tracker: Locations</a></li>
                    </ul>
                </div>  
            </nav>
            <div class="row container-fluid m-0 pt-3 pb-3 px-5">
                <h4 class="text-center">Order Database</h4>
            </div>
            <div class="row container-fluid m-0 pt-3">
                <table class="table table-dark table-hover">
                    <thead>
                        <td>Order ID</td>
                        <th>Customer ID</th>
                        <th>Customer Name</th>
                        <th>Location ID</th>
                        <th>Location Name</th>
                        <th>Delete Order</th>
                    </thead>

                    <tbody>
                        {{#each orders}}
                        <tr>
                            <td>{{order_id}}</td>
                            <td>{{customer_id}}</td>
                            <td>{{customer_name}}</td>
                            <td>{{location_id}}</td>
                            <td>{{location_name}}</td>
                            <td><button type="button" class="btn btn-outline-primary" onclick="deleteOrder({{order_id}})">Delete</button></td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
            <div class="input-group m-0 mt-2 px-5">
                <label for="new-order" class="form-label">Add a new order:</label>
            </div>
            <form class="input-group m-0 mt-2 px-5 mb-5" id="addorder" action="/orders" method="post">
                <select class="form-select form-select-sm" name="customer_id">
                    <option selected>Choose customer</option>
                    {{#each customers}}
                    <option value="{{co_id}}">{{co_id}} - {{first_name}} {{last_name}}</option>
                    {{/each}}
                </select><br>
                <select class="form-select form-select-sm" name="location_id">
                    <option selected>Choose location</option>
                    {{#each locations}}
                    <option value="{{lo_id}}">{{lo_id}} - {{city_name}}, {{state_name}}</option>
                    {{/each}}
                </select><br>
                <input class="btn btn-outline-primary" type="submit" value="Add">
            </form>
        </div>
    </body>
</html>